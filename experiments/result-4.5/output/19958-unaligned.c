/Users/wpf/Documents/workspace/text-filter/test/linux-4.5/arch/tile/kernel/unaligned.c
void jit_bundle_gen(struct pt_regs *regs, tilegx_bundle_bits bundle,
{
default:
get_user/put_user" macros defined in "uaccess.h".
get_user() or put_user(). If the macro returns non-zero value,
get_user(x, ptr) */
get_user(b, ptr++);
}
void do_unaligned(struct pt_regs *regs, int vecnum)
{
case PR_UNALIGN_SIGBUS:
get_user/put_user() macros. Panic the
get_user(bundle, pc) != 0) {
}
